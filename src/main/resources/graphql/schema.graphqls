type User {
    id: ID!
    name: String!
    email: String!
    posts: [Post]!
}

type Post {
    id: ID!
    title: String!
    content: String!
}

type Query {
    getAllUsers: [User]!
    getUserById(id: ID!): User
}